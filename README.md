# Solaris Protocol JS Library

JavaScript client library for interacting with Solaris Protocol.

## Installation

Using npm:

```
npm install @solaris-protocol/solaris-ts
```

Using yarn:

```
yarn add @solaris-protocol/solaris-ts
```

## Usage

### Init Lending Market